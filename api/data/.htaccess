# Bloqueia acesso HTTP direto ao state.json (PHP lê pelo disco)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
